spring:
  application:
    name: team1-front
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  config:
    import: optional:file:.env[.properties]

server:
  port: ${SERVER_PORT:10404}

management:
  endpoints:
    web:
      exposure:
        include: health, service-registry
  endpoint:
    health:
      show-details: never

gateway:
  url: http://localhost:10400

juso:
  confirm-key: ${JUSO_CONFIRM_KEY:}

security:
  jwt:
    issuer: ${JWT_ISSUER:account-api}
    access-token-ttl: ${JWT_ACCESS_TOKEN_TTL:30m}
    allowed-algorithms:
      - rs256
    jwk-set-uri: ${JWK_SET_URI:http://localhost:10413/.well-known/jwks.json}

app:
  api-base-url: ${API_BASE_URL:} # prod 환경에서는 빈 값으로(상대 경로 적용)
