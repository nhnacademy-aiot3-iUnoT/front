spring:
  application:
    name: team1-front

server:
  port: ${SERVER_PORT:10404}

management:
  endpoints:
    web:
      exposure:
        include: health, service-registry
  endpoint:
    health:
      show-details: never

gateway:
  url: http://localhost:10400
